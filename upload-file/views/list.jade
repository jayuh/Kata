doctype
html
	head
		meta(charset="utf-8")
		title 上传文件列表
		link(rel="stylesheet" href="/css/style.css")
	body
		h1 文件列表页
		nav
			ul.c
				li
					a(href="/") 首页
				li
					a(href="/list") 文件列表页
		- if (list.length === 0)
			p 没有上传任何文件
		- else
		ul.file-list
			each item in list
				li
					a(href="/#{item}") #{item}
					span 
					a(href="/delete/#{item}").del 删除