/**
 * @description {{ name_zh }} - ViewModel
 * Created by {{ um }} on {{ date }}.
 */

define([
    'glass',
    'common',
    'avalon',
    'app/{{ grouping }}/model/{{ name }}-model'
], function (Class, common, avalon, model) {
    'use strict';
    var Index = Class.create({
        
        initialize: function () {
            // TODO 这是一个新建的ViewModel模块，请从这里开始，并修改此段注释！
        }

    });

    return Index;
});
